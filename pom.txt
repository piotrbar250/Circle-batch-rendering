    now i create device pointer check if it's correct:

    void computeNextFrame(void** cuda_vbo_resource)
    {
        glm::vec2* dptr = cuda_functions::getMappedPointer(cuda_vbo_resource);

        cuda_functions::computeNextFrameInit(boidsCount, device_positions, device_velocities, device_newPositions, device_newVelocities, device_accelerations, device_translations);
        cuda_functions::swapFrames(boidsCount, device_positions, device_velocities, device_newPositions, device_newVelocities);
        cuda_functions::getDataFromGPU(boidsCount, device_translations, translations.data());
    }

    glm::vec2* getMappedPointer(void **cuda_vbo_resource)
    {
        cudaGraphicsMapResources(1, (cudaGraphicsResource**)cuda_vbo_resource, 0);
        size_t num_bytes;
        glm::vec2* devPtr;
        cudaGraphicsResourceGetMappedPointer((void**)&devPtr, &num_bytes, (cudaGraphicsResource*)cuda_vbo_resource); // which one
        cudaGraphicsResourceGetMappedPointer((void**)&devPtr, &num_bytes, (cudaGraphicsResource*)*cuda_vbo_resource);
        return devPtr;
    }

    these are the same?
    *(cudaGraphicsResource**)cuda_vbo_resource
    (cudaGraphicsResource*)*cuda_vbo_resource